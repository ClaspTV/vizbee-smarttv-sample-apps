import AVPlayer from '../../common/player/AVPlayer';

export default class TizenPlayer extends AVPlayer {

    constructor(platform, platformConfig) {
        
        super(platform, platformConfig);
    }
}