## Summary

<!-- What does this PR change and why? -->

## Builds

> The opt-in checkboxes below trigger packaged builds on demand. Each box
> auto-unchecks whenever new commits are pushed to this PR, so the prior
> approval never applies to fresh code — re-check after a push to build the
> latest commit.

- [ ] Generate Xbox Build
