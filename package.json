{
  "name": "vizbee-sample-webapp",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-TV sample web app (VizioSmartCast / SamsungTizen / LGWebOS / Xbox) demonstrating clean platform abstraction, feature flags, and a stub-first Vizbee SDK integration seam.",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview --host --outDir dist/desktop",
    "typecheck": "tsc --noEmit",
    "build": "npm run build:desktop",
    "build:desktop": "tsc --noEmit && vite build --mode desktop",
    "build:vizio": "tsc --noEmit && vite build --mode viziosmartcast",
    "build:tizen": "tsc --noEmit && vite build --mode tizen && bash scripts/build-tizen.sh",
    "build:webos": "tsc --noEmit && vite build --mode webos && bash scripts/build-webos.sh",
    "build:xbox": "tsc --noEmit && vite build --mode xbox && bash scripts/build-xbox.sh",
    "build:all": "bash scripts/build-all.sh",
    "pack:tizen": "bash scripts/build-tizen.sh",
    "pack:webos": "bash scripts/build-webos.sh",
    "debug:webos": "bash scripts/debug-webos.sh",
    "start:desktop": "vite build --mode desktop && vite preview --outDir dist/desktop --host",
    "start:vizio": "vite build --mode viziosmartcast && vite preview --outDir dist/viziosmartcast --host",
    "start:tizen": "vite build --mode tizen && vite preview --outDir dist/tizen --host",
    "start:webos": "vite build --mode webos && vite preview --outDir dist/webos --host",
    "start:xbox": "vite build --mode xbox && vite preview --outDir dist/xbox --host",
    "deploy:s3": "CLOUDFRONT_DISTRIBUTION_ID=${CLOUDFRONT_DISTRIBUTION_ID:-E12EF2Z11SO5GL} bash scripts/deploy-s3.sh",
    "ship:tizen": "tsc --noEmit && vite build --mode tizen && npm run deploy:s3 tizen",
    "ship:webos": "tsc --noEmit && vite build --mode webos && npm run deploy:s3 webos",
    "ship": "tsc --noEmit && vite build --mode tizen && vite build --mode webos && npm run deploy:s3 tizen webos"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "@vizbee/sdk-tizen": "file:packages/sdk-tizen",
    "@vizbee/sdk-webos": "file:packages/sdk-webos"
  }
}
